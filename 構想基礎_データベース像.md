# テーマ提案

- 飲食店予約管理アプリ

## 予約管理メニュー

```
トップページ
　├─予定タイムライン
　│　└─
　│
　├─
　│　└─
　│
　├─
　│　└─
　│
　├─
　│　└─
　└─ログイン
```

### データベース内容

| テーブル           | 役割                           |
| ------------------ | ------------------------------ |
| `reservations`     | 予約システムそのもの　　       |
| `reservation_seat` | 予約と席を紐づける中間テーブル |
| `seats`            | 席マスタ　　　　 　　　        |
| `shop_settings`    | 店舗設定　　　　 　　　        |
| `admin`            | ユーザー,店舗管理等            |

```
- reservations (予約情報)
　├─id
　├─customer_name (氏名)
　├─people (人数)
　├─reservation_date (日付)
　├─start_time (利用開始時間 MM/DD TIME)
　├─end_time (利用終了時間 MM/DD TIME)
　├─phone (ご連絡先)
　├─status (予約状況)(reserved:予約済み,cancelled:キャンセル,temporary:仮予約
          ※以下は要らない？　seated:来店・着席, completed:利用終了)
　├─description (メモ)


- reservation_seat (予約と席を紐づける中間テーブル)
　├─id
　├─reservation_id (🔓reservations.id)
　└─seat_id (🔓SEAT.id)

- seats(席マスター情報)
　├─id (🔓seat_id)
　├─seat_name
　├─type (カウンター・テーブル・座敷)
　├─capacity (人数制限)
　├─is_active (利用状況)
　├─display_order (表示順)

- shop_settings (各店舗の設定：当面は１店舗で良い)
　├─id ()
　├─shop_name (店舗名)
　├─business_start (営業開始)
　├─business_end (営業終了)
　├─slot_minutes (予約時間単位：まずは３０分単位で固定)

- (ADMIN)
　├─(ユーザー管理)
　├─(店舗管理)

```

#DB作成手順

# 1. 開発用の `reservations` フォルダを作る

Laravelプロジェクトをまとめて置くための開発用フォルダを作ります。

---

# 2. Laravel 12のプロジェクトを作る

Laravel 13などの最新版ではなく、Laravel 12系に固定します。

`reservations` フォルダに移動していることを確認してから、

```cmd
composer create-project laravel/laravel reservations "^12.0"
```

を実行

---

# 3. Laravel 12になっているか確認する

作成されたプロジェクトへ移動します。

```cmd
cd reservations
```

Laravelのバージョンを確認します。

```cmd
php artisan --version
```

次のようにLaravel 12系が表示されることを確認してください。

```text
Laravel Framework 12.x.x
```

---

# 4. フロント側の依存パッケージをインストールする

プロジェクトフォルダ内で、

```cmd
npm install
```

を実行します。

これによって `package.json`
をもとに、フロントエンド側で必要なパッケージがインストールされます。

`node_modules` フォルダが作られます。

---

# 5. Viteを起動する

```cmd
npm run dev
```

を実行します。

これはViteの開発サーバーを起動するコマンドです。

Viteは主に、

- JavaScript
- CSS
- フロントエンドの変更監視
- 開発中の自動反映

などを担当します。

このターミナルは作業中、そのまま起動しておきます。

終了するときは、

```text
Ctrl + C
```

です。

---

# 6. Laravel側の開発サーバーを起動する

**新しいCMD（ターミナル）をもう一つ開きます。**

プロジェクトへ移動します。

```cmd
cd "C:\Users\ユーザー名\Documents\develop\reservations"
```

Laravel側を起動します。

```cmd
php artisan serve
```

通常はブラウザから、

```text
http://127.0.0.1:8000
```

など、Artisanが表示したURLへアクセスします。

Laravelの画面が表示されればOKです。

---

# 7.phpMyAdminでデータベースを作る

XAMPPで、

```text
Apache → Start
MySQL  → Start
```

にします。

phpMyAdminを開き、Laravel用の**データベースだけ**を作成します。

例：

```text
reservations
```

最初はテーブルを手作業で作りません。

Laravelでは後ほど**マイグレーション**を使ってテーブルを作成します。

---

# 8. `.env` にDB接続情報を書く

Laravelプロジェクト直下の、

```text
.env
```

を開きます。

XAMPPのMariaDBを使用する設定例：

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=reservations
DB_USERNAME=root
DB_PASSWORD=
```

---

# 9. DB接続を確認する

Laravelプロジェクト内で、

```cmd
php artisan tinker
```

を実行します。

Tinkerが起動したら、DB接続を確認できます。

```php
DB::connection()->getPdo();
```

PDOオブジェクトが返れば、LaravelからDBへ接続できています。

終了するときは、

```text
exit
```

と入力します。

---

# 10. VS Codeでプロジェクトを開く

プロジェクトフォルダ内にいる状態なら、

```cmd
code .
```

でVS Codeを開ける環境もあります。

利用できない場合は、VS Codeを起動して、

**ファイル → フォルダーを開く**

から、

```text
reservations
```

を選択してください。

---

### モデルとマイグレーションファイルを作成

```bash
php artisan make:model Reservation -m
php artisan make:migration create_reservation_seat_table
php artisan make:model Seat -m
php artisan make:model ShopSetting -m
php artisan make:model Admin -m
```

```text
Reservation

```

```text
reservation_seat
```

```text
Seat
```

```text
ShopSetting
$table->id();
$table->string('shop_name'); // 店舗名
$table->time('business_start'); // 営業開始
$table->time('business_end'); // 営業終了
$table->integer('slot_minutes'); // 表示単位(15分,30分など)
$table->json('closed_days')->nullable(); // 店休日
$table->timestamps();
```

```text
Admin
$table->id();
$table->string('name'); // 名前
$table->string('email')->unique(); // メールアドレス
$table->string('password');
$table->enum('role', ['admin', 'staff']);
$table->enum('status', ['active', 'inactive']);
$table->timestamps();

```

```php
php artisan migrate
```

CSVを読み込む
